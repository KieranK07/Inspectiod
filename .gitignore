node_modules/
dist/
build/
*.log
.env
.DS_Store

# captured session data is local-only, don't commit it
sessions/*
!sessions/.gitkeep

# persistent browser profile holds real login cookies/sessions, never commit
browser-profile/
